# MAXWELL

一个可以直接发布到 GitHub Pages 的静态测试网页。

## 发布

1. 在 GitHub 新建一个名为 `maxwell` 的公开仓库。
2. 将本目录中的文件提交并推送到仓库的 `main` 分支。
3. 打开仓库的 `Settings > Pages`。
4. 在 `Build and deployment` 中选择 `Deploy from a branch`。
5. 选择 `main` 和 `/ (root)`，然后保存。

发布地址为 `https://你的GitHub用户名.github.io/maxwell/`。
